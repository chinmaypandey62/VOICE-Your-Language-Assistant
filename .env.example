# Backend API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# MongoDB Configuration
# Replace with your MongoDB connection string
# For local MongoDB: mongodb://localhost:27017/voice-assistant
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/voice-assistant
MONGODB_URI=mongodb://localhost:27017/voice-assistant

# JWT Configuration
# Generate a strong random secret for JWT tokens
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Development Settings (remove in production)
# Uncomment the next line to bypass authentication during development
# NEXT_PUBLIC_BYPASS_AUTH=true

# Optional: Set NODE_ENV for production optimizations
# NODE_ENV=production
